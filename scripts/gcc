#!/bin/bash
# GCC wrapper — strips clang-specific flags, delegates to real gcc
REAL_GCC=/usr/bin/gcc
args=()
for a in "$@"; do
    case "$a" in
        -Wno-error=unaligned-access|-Wno-unused-const-variable|-Wno-sometimes-uninitialized) ;;
        -mno-outline-atomics|-mno-implicit-float) ;;
        *) args+=("$a") ;;
    esac
done
exec "$REAL_GCC" "${args[@]}"
